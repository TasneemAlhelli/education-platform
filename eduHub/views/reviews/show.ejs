<%- include('../partials/header') %>
  <h2>Reviews</h2>
  <% reviewList.forEach((value)=> { %>
    <p>
      <%= value.message %>
    </p>
    <p>
      <%= value.rating %>
    </p>
    <% }); %>
      <form action="/classes/myclasses" method="GET">
        <button type="submit">Back</button>
      </form>
      <%- include('../partials/footer') %>